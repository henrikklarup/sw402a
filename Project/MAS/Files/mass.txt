/* Initializes the game with the properties
Maximum Units = 400
*/
Main(400)
{	
	/* Creates two teams with the properties
	Name = Aliens / Team Rocket
	Color = Hex code of the colors Red and Green */
	new Team teamAliens("Aliens", "#FF0000");
	new Team teamRocket("Team Rocket", "#00CC00");
	
	new Agent agentJohn("John", 5, teamAliens);
	new Agent agentJane("Jane", 5, teamAliens);
	new Agent agentBob("Bob", 5, teamAliens);
	new Agent agentAlice("Alice", 5, teamAliens);

	/* Creates a squad with the properties
	Name = Squad Aliens
	Team = teamAliens 
	Binds the squad to teamAliens */
	new squad squadAliens("Squad Aliens", teamAliens);	

	new squad squadPredator("Squad Predator", teamAliens);
	new squad squadA("Alfa", teamRocket);
	new squad squadB("Beta", teamRocket);
	new squad squadC("Gamma", teamRocket);
	new squad squadD("Delta", teamRocket);
	
	squadAliens.add(agentJohn);
	squadAliens.add(agentJane);

	squadPredator.add(agentBob);
	squadPredator.add(agentAlice);

	if(1 != 1)
	{}

	for(num i = 0; i < 20; i = i + 1)
	{
		/* Creates a new agent with the properties
		Name = unit
		Rank = 1
		Team = teamRocket
		If there is more units with the same name, 
		the new unit will get a unique name */
		new Agent newAgent("unit", 1, teamRocket);
		
		if(i < 20/4)
		{
			squadA.add(newAgent);
		}
		if(i < (20/4)*2)
		{
			squadB.add(newAgent);
		}
		if(i < (20/4)*3)
		{
			squadC.add(newAgent);
		}
		else
		{
			squadD.add(newAgent);
		}
	}
		
	new actionPattern circle();
	
	string actionUp = "unit move up";
	string actionDown = "unit move down";
	string actionLeft = "unit moveleft";
	string actionRight = "unit move Right;
	string actionHold = "unit move hold";

	circle.add(actionLeft);
	circle.add(actionDown);
	circle.Add(actionDown);
	circle.add(actionRight);
	circle.add(actionRight);
	circle.add(actionUp);
	circle.add(actionUp);
	circle.add(actionLeft);

}